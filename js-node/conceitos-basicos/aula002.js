// Variáveis

const meuNome = "Murilo"; // Variável que não muda
let cidade = "Piçarras"; // Variável que pode mudar
var cor = "vermelho"; // Serve para todos os scopos

console.log(meuNome);
console.log(cidade);

{
    console.log(cor);
}